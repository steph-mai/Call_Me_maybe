[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Introduction to function calling in LLMs"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0.0",
    "pydantic>=2.0.0",
    "llm-sdk",
    "pytest", 
    "flake8", 
    "mypy"
]

[tool.uv.sources]
llm-sdk = { path = "./llm_sdk" }
